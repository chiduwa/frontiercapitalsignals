# For LOCAL testing only. In production this bot runs via GitHub Actions
# (.github/workflows/trading-bot-cycle.yml) using repo secrets/variables,
# not this file — see README.

# Trade-only key: enable Futures + Reading, leave Withdrawals OFF.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Keep true until you've reviewed several days of dry-run job logs
# (Actions tab -> Trading Bot Cycle) and are satisfied with what it
# WOULD have done.
DRY_RUN=true

# Same D1 database the rest of this repo's scripts use.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
FCS_D1_DATABASE_ID=

# --- Everything below has a sane default in src/config.mjs — only set
# what you actually want to change from the defaults. ---

# MIN_POSITION_PCT=0.05
# MAX_POSITION_PCT=0.20
# MAX_TOTAL_EXPOSURE_PCT=0.50
# MIN_LEVERAGE=3
# MAX_LEVERAGE=20
# MIN_CONFIDENCE_RATIO=0.55
# MIN_TOP_INDICATOR_ACCURACY=0.55
# ENTRY_LOW_RANGE_POS=0.25
# ENTRY_HIGH_RANGE_POS=0.75
# FEAR_GREED_EXTREME_LOW=15
# FEAR_GREED_EXTREME_HIGH=85
# EXTREME_AGGRESSION_BOOST=1.25
# STOP_LOSS_MARGIN_FRACTION=0.5
# COOLDOWN_MINUTES=60
# CIRCUIT_BREAKER_DRAWDOWN_PCT=0.15
# DAILY_LOSS_LIMIT_PCT=0.10
# MAX_FUNDING_RATE_ABS=0.001
